import { NoWhitespacesDirective } from './no-whitespaces.directive';

describe('NoWhitespacesDirective', () => {
  it('should create an instance', () => {
    const directive = new NoWhitespacesDirective();
    expect(directive).toBeTruthy();
  });
});
